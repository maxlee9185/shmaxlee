import 'package:flutter/material.dart';

class shmaxleeHomePage extends StatelessWidget {
  const shmaxleeHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Seonghyun HomePage'),
      ),
      body: const Center(child: Text('center')),
    );
  }
}
